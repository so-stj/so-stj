name: Main
on:
  workflow_dispatch:

  push:
    branches:
      - main
  
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: metrics embed
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.plugin.language.svc
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_languages: yes
